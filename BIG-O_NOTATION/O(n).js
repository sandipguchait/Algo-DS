// O(n) ==> Linear time --> 
//Number of element increases is same as number of operation increases propotionally 
 
const boxes = [ 1, 2, 3, 5, 6, 7];

function compressAllBoxes(boxes) {
  boxes.forEach(item => {
    console.log(item)
  });
};

compressAllBoxes(boxes);