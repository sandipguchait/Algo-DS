// O(n) ==> Linear time --> 
// If Number of element increases then the  number of operation increases propotionally 
 
const boxes = [ 1, 2, 3, 5, 6, 7];

function compressAllBoxes(boxes) {
  boxes.forEach(item => {
    console.log(item)
  });
};

compressAllBoxes(boxes);